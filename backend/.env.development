# Development Environment

# Host Configuration
HOST_IP=10.0.0.129
HOST_DOMAIN=localhost

# General Configuration
PORT=3443
HTTP_PORT=3000
NODE_ENV=development
API_BASE_URL=https://${HOST_IP}:3443/api
FRONTEND_URL=https://${HOST_IP}:5173

# Database Configuration
DB_USER=sa
DB_PASSWORD=Kacu938861
DB_SERVER=host.docker.internal/ARCHASERVER
DB_NAME=WebAppTEST
DB_PORT=1433

# JWT Configuration
ACCESS_TOKEN_SECRET=cDb!Yu6wU!2

# Storage Configuration
STORAGE_TYPE=local                     # 'local' o 'remote'
LOCAL_STORAGE_PATH=/usr/src/app/uploads
REMOTE_STORAGE_PATH=//SERVER_IP/share_name
REMOTE_STORAGE_TYPE=mounted            # 'mounted', 'smb', o 'nfs'

# SMB Configuration (per remote storage)
SMB_PATH=//192.168.1.100/share/folder
STORAGE_DOMAIN=DOMAIN
STORAGE_USERNAME=username
STORAGE_PASSWORD=password

# NFS Configuration (per remote storage)
NFS_SERVER=192.168.1.100
NFS_PATH=/export/share

# UI Configuration
SWEET_ALERT_CANCEL_COLOR=rgb(155, 33, 33)
SWEET_ALERT_CONFIRM_COLOR=rgb(30, 97, 155)
SWEET_ALERT_CANCEL_TEXT=Annulla
SWEET_ALERT_Z_INDEX=9999

# Printer Configuration
PRINTER_IP=tcp://192.168.1.101
PRINTER_TEST_MODE=true

# SMTP Configuration
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=USERNAME
SMTP_PASSWORD=PASSWORD
SMTP_FROM=pippo@mail.it

# AI Configuration
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_MODEL=claude-3-haiku-20240307
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# AI Options
AI_FALLBACK_STRATEGY=openai-only  # Opzioni: claude-only, openai-only, claude-then-openai
AI_MAX_TEXT_LENGTH=10000
AI_MAX_SUMMARY_POINTS=30
AI_MIN_POINT_LENGTH=10

# HTTPS Configuration
HTTPS_PORT=3443
HTTPS_KEY_PATH=/usr/src/app/ssl/key.pem
HTTPS_CERT_PATH=/usr/src/app/ssl/cert.pem